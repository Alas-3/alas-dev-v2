// next-sitemap.js
/** @type {import('next-sitemap').IConfig} */
const config = {
    siteUrl: 'https://acelabador.vercel.app', // Change to your site URL
    generateRobotsTxt: true, // (optional)
    // Optional, if you have a specific change frequency for certain pages
    // exclude: ['/excluded-page'], // Exclude any specific pages from the sitemap
    // Additional configuration options can go here
  };
  
  module.exports = config;
  